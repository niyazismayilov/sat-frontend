query SliderShows($sort: [String]) {
  sliderShows(sort: $sort) {
    data {
      id
      attributes {
        title
        coverImage {
          data {
            id
            attributes {
              name
              url
            }
          }
        }
        content
      }
    }
  }
}